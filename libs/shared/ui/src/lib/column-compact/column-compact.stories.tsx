import type { Meta } from '@storybook/react';
import { ColumnCompact } from './column-compact';

const Story: Meta<typeof ColumnCompact> = {
  component: ColumnCompact,
  title: 'ColumnCompact',
};
export default Story;

export const Primary = {
  args: {},
};
