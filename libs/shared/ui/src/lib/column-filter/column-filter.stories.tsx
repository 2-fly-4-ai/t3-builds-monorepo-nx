import type { Meta } from '@storybook/react';
import { ColumnFilter } from './column-filter';

const Story: Meta<typeof ColumnFilter> = {
  component: ColumnFilter,
  title: 'ColumnFilter',
};
export default Story;

export const Primary = {
  args: {},
};
